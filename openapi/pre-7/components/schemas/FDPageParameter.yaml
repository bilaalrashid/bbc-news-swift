name: page
in: query
description: >-
  The ID of the page to fetch.

  Global examples include `"chrysalis_discovery"`, `"front_page"`,
  `"live_radio"`, `"media_page"`, `"topic_menu"`
required: true
schema:
  type: string
  example: chrysalis_discovery
